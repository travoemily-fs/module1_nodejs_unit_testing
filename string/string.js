const uppercase = (str) => {
    return str.toUpperCase();
}
// if you already have a return you don't need to type it twice
const lowercase = (str) => str.toLowerCase();

module.exports = {
    uppercase,
    lowercase
}